import { ColorRainbowDirective } from './color-rainbow.directive';

describe('ColorRainbowDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorRainbowDirective();
    expect(directive).toBeTruthy();
  });
});
